# "מאז הייבוא" — מועמד משולב לפני נעילה

בסיס: גרסת מודול העדכון של קלוד, עם השינויים שסוכמו לאחר ההשוואה. זו **אינה נעילה חדשה** עדיין.

## מבנה
- `Gadget%20page%20tool.update.js` — כל לוגיקת "מאז הייבוא", המלל והעיצוב.
- `Gadget%20page%20tool.js` — רק הפקד והטעינה העצלנית של המודול.
- ששת קבצי הייצור האחרים לא שונו.

## זרימה
- הפקד מוצג רק כששדה `גרסה` הוא מזהה חיובי תקין, גרסת הייבוא אינה מסומנת כמחוקה, והגרסה הנוכחית שונה ממנה.
- לפני לחיצה: אין קריאה נוספת ואין טעינת קובץ העדכון.
- בלחיצה הראשונה נטענות במקביל היסטוריית המכלול וגרסאות ויקיפדיה.
- `compare` נטען רק בלחיצה על "הצג הבדלים". הכפתור מוצג רק למשתמש שחבר גם ב־`wikiupdate` וגם ב־`aspaklaryaEditor`; נתוני "מאז הייבוא" עצמם אינם מוגבלים לקבוצות האלה.
- יעד ה־diff הוא הגרסה שנמדדה בפועל בספירה, כדי שהספירה והטבלה יתארו אותו טווח.

## זמן הייבוא/העדכון
1. מחפשים בהיסטוריית המכלול עדכון ממוספר שמספר הגרסה שלו תואם **בדיוק** לשדה `גרסה`.
2. במקביל נשמר העדכון הממוספר האחרון. אם הוא מציין גרסה אחרת, מוצגת התרעת אי־התאמה גם כאשר נמצא עדכון תואם ישן יותר.
3. אם יש עדכונים ממוספרים אך אין אף התאמה — מוצגת שורת "אין מידע" מפורשת והתרעת אי־התאמה.
4. תקציר ייבוא משמש fallback רק אם אין בהיסטוריה **אף עדכון ממוספר**.
5. אם אין גם ייבוא — מוצגת שורת "אין מידע" מפורשת.

## מקורות ויקיפדיה
כל הקריאות משתמשות ב־`core.netGet`, ולכן חוזה ה־timeout/retry/סיווג הכשל נשאר של הליבה.

במקור ברירת המחדל נשמר הפיצול של הסקריפט הישן:
- היסטוריית גרסאות: `/import/get_wik1i.php`
- `compare`: `https://import.hamichlol.org.il/`

במצב `userjs-import-source=direct`:
- שתי הבקשות: `https://he.wikipedia.org/w/api.php`

הכתובות הקבועות מרוכזות בראש קובץ העדכון ומתועדות כאן, כדי ששינוי שרת עתידי לא יישכח.

## דפדוף
- שליפת גרסאות משתמשת ב־`rvlimit=500`.
- אובייקט `continue` מועבר בשלמותו לעמוד הבא.

## כשלים
- כל תת־בדיקה עצמאית; כשל באחת אינו מוחק מידע שהתקבל באחרת.
- retry מוצג רק כאשר `err.transient === true`.
- כשל קבוע אינו מקבל retry.
- ממצא ידוע (למשל גרסת הייבוא אינה בהיסטוריה) גובר על הודעת כשל כללית.
- בכשל כללי מוצגת הודעת הליבה עצמה; אם אין הודעה שימושית מוצג fallback כללי `שגיאה בבדיקה`.

## diff
- נשמר מנגנון הציור של גרסת קלוד: אין בנייה מחדש של הכפתור ולכן ה־listener נשמר.
- הטבלה כוללת `colgroup` עם ארבע עמודות ואת `diff-contentalign-right`.
- אין כפיית `dir=ltr`.

## בדיקות במועמד הנוכחי
- `node contract-tests/run.js` — **25/25** עברו.
- `python update-tests/browser_integration.py` — **16/16** עברו לאחר עדכון סביבת הבדיקה והוספת שני תרחישי ההרשאות, ושוב על החבילה הנוכחית (21 בספטמבר 2026). נבדק במפורש שללא שתי הקבוצות נתוני "מאז הייבוא" מוצגים, אין כפתור diff ואין `compare`, ושקבוצה אחת בלבד אינה מספיקה.
- `node update-tests/run.js` — **30/30** עברו על החבילה הנוכחית (21 בספטמבר 2026), בדפדפן מדומה עם ג'יי־קוורי אמיתי. עזרי הכשל המדומים נמצאים ברמה העליונה, והציפיות משקפות את הודעות הליבה.
- בסיס התמונות/חתימות של הנעילה **לא עודכן**. זה ייעשה רק לאחר בדיקה חיה ואישור סופי.

## בדיקה חיה מהסביבה הנוכחית
- מצב `direct`: API הגרסאות של ויקיפדיה מקבל `rvlimit=500`, ו־`compare` מקבל `fromrev`/`torev` מפורשים.
- שני נתיבי ברירת־המחדל של המכלול (`/import/get_wik1i.php` ו־`import.hamichlol.org.il`) אינם נגישים מכלי הגלישה החיצוני בסביבה זו, ולכן עדיין נדרשת בדיקה מתוך אתר המכלול לפני נעילה.

## לפני נעילה
נדרשת בדיקה חיה של:
1. `/import/get_wik1i.php` עם יותר מ־500 גרסאות ו־continuation.
2. `compare` דרך שרת הייבוא עם `torev` מפורש.
3. מצב direct.
4. כיוון טבלת ההבדלים ומראה במסך צר.

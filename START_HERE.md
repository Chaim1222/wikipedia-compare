# Page tool — נקודת פתיחה לשלב הבא

## מצב
זהו הבסיס הסופי והנעול של רפקטור שלב 6, לאחר בדיקה חיה באתר.
שלב 7 בוטל. אין להשתמש ב־`backlinks.js`; עץ המקשרים נמצא בתוך `details.js`.

## קבצי הייצור הפעילים
- `Gadget%20page%20tool.js`
- `Gadget%20page%20tool.core.js`
- `Gadget%20page%20tool.card.js`
- `Gadget%20page%20tool.details.js`
- `Gadget%20page%20tool.preview.js`
- `Gadget%20page%20tool.messages.js`
- `Gadget%20page%20tool.css`
- `Gadget%20page%20tool.update.js`
- `Gadget%20page%20tool.links.js` (חלון תיקון הקישורים, בטעינה עצלה)
- `Gadget%20page%20tool.redirects.js` (הפניות מוויקיפדיה ודף שאינו קיים, בטעינה עצלה)

## מה כבר הושלם
- חילוץ `core` ללא שינוי בלוגיקת ההכרעה.
- מנוע כרטיסים מאוחד.
- מנגנון פעולות מאוחד.
- בונה פאנל פרטים מאוחד.
- `preview` ו־`details` בטעינה עצלה.
- 25 תרחישי חוזה ל־core עוברים.
- נבדק חי באתר: fallback גרסה → Wikidata → כותרת; העברה תוך דריסת הפניה; העברה תוך מחיקת ערך; הפיכת ערך להפניה.
- ששת ממצאי הסקירה נסגרו: כפילות אזהרה, אזהרה שנבלעה, כשל הרשאות, שמירת `#פסקה` בריחוף, retry ל־details, ורישום שגיאות ל־Console.
- אזהרת הרשאות מוצגת רק בכרטיסים שבהם כשל הרשאות יכול להסתיר פעולה.

## תיקונים אחרי נעילת שלב 6
- תיקון 1: כשל רגעי בטעינת משאבי הכרטיס אינו מוצג עוד כ"הבדיקה נכשלה". ראו `FIX1_ASSET_RETRY.md`. שונה רק הקובץ הראשי.

## סבב הפניות, העברות ושמות (ממתין לאישור מלל, לבדיקות ולנעילה)
- תיקון החירום בשער הוסר. דף שהוא הפניה עובר למסלול ייעודי, בלי העברה ובלי מחיקה.
- ההפניות אל דף שעומד לזוז מוצגות לפני הפעולה. עדכון שלהן מכרטיס ההצלחה מוצע רק אחרי העברה בלי הפניה (שם הן נשברות); עם הפניה או בהפיכה להפניה הן הופכות לכפולות, וכלים אוטומטיים מטפלים בהן. ראו פרק 8 ב־`REDIRECTS.md`.
- שאלת ההפניה נשאלת על השם הישן בוויקיפדיה, וכללי השמות פועלים בשני הכיוונים.
- פירוט, מלל לאישור ותוצאות: `REDIRECTS.md`. נכון לסיום סבב ההפניות (לפני סבב תיקון הקישורים): כרטיסים: 226 תרחישים, אפס כשלי כוונה; 14 תרחישים חדשים ממתינים לתמונות ייחוס, ו־120 הבדלים מוסברים מול הקיימות. מבחני רגישות: 31 מוגדרים; תשעת החדשים והמעודכנים הורצו לפני הנעילה ונתפסו ברמה הצפויה.

## סבב תיקון קישורים (ממתין לבדיקה חיה, לאישור מלל ולנעילה)
- אחרי העברה בלי הפניה, חלון תיקון לקישורים הישירים אל השם הישן, בקובץ ייצור תשיעי: `Gadget%20page%20tool.links.js`. זמין רק לחברי הקבוצה `bot`.
- תבניות שמקשרות לדף מוצגות כעובדה: בסיכום הדפים המקשרים, בעץ בפאנל, בשורת האזהרה, ובכרטיס ההצלחה כקישורים לעדכון בדף התבנית. הכלי לא מנחש אילו ערכים מקבלים את הקישור מהן.
- פירוט: `LINK_FIXES.md`. סוויטה ייעודית לחלון: `links-tests/`.
- **שינוי השאילתות בחבילה השישית (שאילתה נפרדת לתבניות) לא הורץ עדיין.** תוצאות החבילה החמישית (23 בספטמבר 2026): `contract-tests` 25/25; `update-tests` 30/30; `links-tests` 73/73; `card-tests`: 231 תרחישים, **אפס כשלי כוונה**, 150 הבדלים מתמונות הייחוס ו־19 תרחישים בלי תמונת ייחוס. כל ההבדלים סווגו בהשוואה ישירה מול הגרסה שבאתר (ראו `LINK_FIXES.md`). תמונות הייחוס לא עודכנו בכוונה; יעודכנו אחרי הבדיקה החיה. מבחני הרגישות לא הורצו, כי הם רצים רק על הבסיס הנעול.

## סבב הפניות מוויקיפדיה ודף שאינו קיים (טרם הודבק; ממתין לאישור מלל, לבדיקה חיה ולנעילה)
- שלוש יכולות מהסקריפט הישן `הפניות.js`: ייבוא הפניות חסרות, ערך נפרד שבוויקיפדיה הוא הפניה, וכרטיס "נראה שהערך קיים במכלול בשם אחר" בדף שאינו קיים (במקום כרטיס הכשל). קובץ ייצור עשירי: `Gadget%20page%20tool.redirects.js`.
- הסקריפט הישן המקוצר נמצא בחבילה (`הפניות.js`) ויש להדביק אותו יחד עם הקובץ הראשי. סדר הדבקה: המודול, המלל, הכרטיס, הליבה, ואז הראשי והסקריפט הישן.
- פירוט, סטיות מהמפרט ותוצאות: `WIKI_REDIRECTS.md`. `card-tests`: 250 תרחישים, אפס כשלי כוונה. מבחני רגישות: 42/42 (בהרצה מול פלט החבילה).

## סבב "מאז הייבוא" (מועמד משולב; ממתין לבדיקה חיה ולנעילה)
- נוסף קובץ ייצור שמיני: `Gadget%20page%20tool.update.js`. בקובץ הראשי נמצאים רק הפקד והטוען. ראו `UPDATE_MODULE.md`.
- המועמד משתמש בהתאמת זמן לפי מספר הגרסה, mismatch גלוי, fallback לייבוא רק כשאין עדכונים ממוספרים, retry רק לכשל רגעי ופיצול endpoints כמו בסקריפט הישן. כפתור ההבדלים מוצג רק לחבר גם ב־`wikiupdate` וגם ב־`aspaklaryaEditor`; נתוני "מאז הייבוא" עצמם נשארים זמינים לכל מי שהכלי נטען אצלו.
- תוצאות על החבילה הנוכחית (21 בספטמבר 2026, כולל סבב ההפניות): `contract-tests` **25/25**, `update-tests/run.js` **30/30**, בדיקת הדפדפן האמיתי **16/16**.
- תמונות הייחוס והבסיס הנעול **לא עודכנו בכוונה**; יעודכנו רק אחרי הבדיקה החיה.

## מערכי הבדיקות
- `contract-tests/` — חוזה מנוע ההכרעה (25 תרחישים). נשאר כפי שהוא.
- `card-tests/` — שכבת הכרטיס והפעולות: בחבילה הנוכחית 250 תרחישים ו־42 מבחני רגישות (בבסיס הנעול: 212 ו־23). ראו `card-tests/README.md` ו-`card-tests/TEST_RESULTS.md`.
- `update-tests/` — "מאז הייבוא": 30 תרחישים בדפדפן מדומה, ובדיקת דפדפן אמיתי של 16 תרחישים.
- `links-tests/` — חלון תיקון הקישורים: פונקציות הזיהוי וההחלפה וזרימות מלאות בחלון. 73/73.

`card-tests/BASELINE_SHA256.txt` נשאר בסיס הנעילה של תיקון 1. `SHA256SUMS.txt` משקף את עשרת קבצי הייצור בחבילה הזו ואת הסקריפט הישן המקוצר. שישה מהם שונים מהבסיס במכוון: הראשי, הליבה, הכרטיס, הפרטים, המלל והעיצוב. קובץ "מאז הייבוא", קובץ חלון התיקון ומודול ההפניות אינם חלק מהבסיס הנעול.

## כלל עבודה מכאן והלאה
1. לפני כל שינוי קוד: שתי הסוויטות ירוקות על הבסיס.
   ```bash
   node contract-tests/run.js
   node card-tests/run.js --require-baseline
   ```
2. אחרי שינוי: הרצה רגילה. כל הבדל מוצג ונבדק לגופו. שינוי מכוון מאושר רק במפורש (`--update-snapshots`), ורק כשבדיקות הכוונה עוברות.
3. לפני החלפת קוד באתר: השוואה מול הגרסה הפעילה (`node card-tests/run.js --compare <תיקייה>`), ובדיקה חיה קצרה באתר.

## התנהגויות ידועות שלא תוקנו
- בביטול שקט של בקשה שלד הטעינה נשאר (`B-error-silent`); השפעה זניחה.
- קישור השפה האנגלית נפתח בחלון בשם `blank` ולא בלשונית חדשה (`target="blank"` בקובץ הראשי).

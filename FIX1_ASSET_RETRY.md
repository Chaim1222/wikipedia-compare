# תיקון 1 — כשל רגעי בטעינת משאבי הכרטיס

בסיס: שלב 6 הסופי והנעול. שונה **רק הקובץ הראשי** (`Gadget%20page%20tool.js`).

## הבעיה

כשהבדיקה מול ויקיפדיה הצליחה, אבל טעינת אחד ממשאבי הכרטיס (סקריפט הכרטיס, קובץ ההודעות או קובץ העיצוב) נכשלה פעם אחת, הכשל עבר למטפל השגיאות הכללי של שרשרת הבדיקה. המטפל הזה נועד לכשלי הכרעה: הוא טען את המשאבים שוב, ואם הטעינה הצליחה, הציג כרטיס "הבדיקה נכשלה — אירעה שגיאה בבדיקת מצב הערך". תוצאת הבדיקה האמיתית אבדה, ורק "נסה שוב" החזיר אותה.

כשל הכרעה וכשל הצגה התערבבו, והמשתמש קיבל מידע שגוי על מצב הערך.

## התיקון

בפונקציה שמציגה תוצאה שהצליחה, כשל בטעינת המשאבים מטופל בנפרד:

1. הכשל נרשם במסוף, ונעשה ניסיון טעינה נוסף.
2. אם הניסיון הנוסף מצליח, מוצגת **תוצאת הבדיקה עצמה**.
3. אם גם הוא נכשל, מוצגת הודעת כשל טעינת המשאבים ("לא ניתן לטעון את משאבי הכרטיס").
4. חריגה בזמן בניית הכרטיס עצמו היא תקלה בקוד, ולא בטעינה. היא ממשיכה למטפל הכללי ולכרטיס הכשל, כמו קודם.

שורת הכותרת של הפונקציה לא שונתה, ולכן נקודות ההזרקה של סוויטת החוזה נשארו תקפות.

## מה השתנה בהתנהגות (לפי סוויטת הכרטיס)

השוואה מלאה מול שלב 6: התנהגות זהה ב-210 מתוך 212 תרחישים. ההבדל רק בשני התרחישים המיועדים:

| תרחיש | לפני | אחרי |
|---|---|---|
| `I-card-script-flaky` — כשל רגעי בסקריפט הכרטיס | כרטיס "הבדיקה נכשלה" | כרטיס "שם הערך שונה בוויקיפדיה" עם פעולת ההעברה |
| `I-stylesheet-flaky` — כשל רגעי בקובץ העיצוב | כרטיס "הבדיקה נכשלה" | כרטיס "שם הערך שונה בוויקיפדיה" |

## מה נשאר זהה, ונבדק במפורש

- כשל קבוע בכל אחד מהמשאבים: אותה הודעת כשל טעינה, אותו מספר ניסיונות ואותו מספר רישומים במסוף (שניים).
- חריגה בזמן בניית הכרטיס: כרטיס כשל, בלי טעינה חוזרת של המשאבים (`I-render-exception-failure-card`, נוסף לסוויטה על שלב 6 לפני התיקון).
- כשל בטעינת הרשאות, כשל הכרעה ו"נסה שוב": ללא שינוי.
- סוויטת החוזה: 25/25.

## שמירה על התיקון

נוספו שני מבחני רגישות, ושניהם נתפסים בבדיקות כוונה:
- `asset-retry-removed` — ביטול הטעינה החוזרת (החזרת הבאג).
- `render-exception-swallowed` — חריגה בבניית הכרטיס שנבלעת כ"כשל טעינת משאבים" (הבאג ההפוך).

## חתימות

| קובץ | לפני | אחרי |
|---|---|---|
| `Gadget%20page%20tool.js` | `6fd608d8d19c6e11ef3c1ee4428a55ce9db1a9d8624eee2eb47f1b8b1080ac42` | `9d52a704e8c0cbbfa13a9d43a5fde2b4f6a080278b3d7ad22848f766a6e94777` |

שאר ששת הקבצים זהים לשלב 6. `SHA256SUMS.txt` ו-`card-tests/BASELINE_SHA256.txt` עודכנו לחתימה החדשה.

## פריסה באתר

לעדכן רק את הקובץ הראשי. אין צורך לשנות את הליבה, הכרטיס, הפרטים, התצוגה המקדימה, ההודעות או קובץ העיצוב.

## בדיקה חיה מומלצת

1. לפתוח כרטיס שינוי שם או הפניה, ולוודא שהוא מוצג ופועל כרגיל, כולל פתיחת הפרטים.
2. לחסום בכלי המפתחים של הדפדפן (לשונית הרשת, חסימת כתובת בקשה) את טעינת סקריפט הכרטיס, ולרענן: צפויה ההודעה "לא ניתן לטעון את משאבי הכרטיס", ושתי שגיאות במסוף. לבטל את החסימה.

כשל רגעי אמיתי (פעם אחת בלבד) קשה לשחזר באתר, והוא מכוסה בשני תרחישי הסוויטה שלמעלה.
